# 2026-08-22 16:26

- 为合并后的 Calcit 0.13.29 迁移准备 pointed-prompt `0.0.8` release。
- 同步 package/deps 版本，CI 显式使用 `calcit,caps` 并校验 canonical Snapshot 格式。
- 本地通过 caps、Snapshot format、JS codegen、Yarn immutable 和 Vite production build。
